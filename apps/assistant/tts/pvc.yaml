apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tts-voices
  namespace: assistant
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      # Piper voices are ~60MB each; this holds plenty.
      storage: 2Gi
